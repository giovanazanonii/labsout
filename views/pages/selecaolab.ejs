<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/selecaolab.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css">
    <title>LABSOUT</title>
    <link rel="icon" href="/imagens/icone.png" type="image/png">
    <style>
        .navbar a {
            color: #fff;
            text-decoration: none;
        }
        .navbar a:hover {
            color: #ddd;
        }
        .navbar-title {
            color: #fff;
            font-size: 20px;
            margin-left: 10px;
        }

         /* Estilos principais */
         .main-content {
            text-align: center;
            margin-top: 50px;
        }

        .button-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }

        .button-wrapper {
            position: relative;
            display: inline-block;
            text-align: center;
        }

        /* Estilo do botão "Ver Mais" */
        .ver-mais {
            display: none;
            position: absolute;
            top: 80%; /* Posiciona abaixo do botão principal */
            left: 50%;
            width: 60%;
            transform: translate(-50%, 10px); /* Centraliza abaixo do botão */
            padding: 5px 10px;
            font-size: 14px;
            background-color: #c7c7c7;
            color: #fff;
            border-radius: 7px;
            cursor: pointer;
            text-decoration: none;
        }


        .button-wrapper:hover .ver-mais {
            display: block;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-lado">
            <a href="/inicialusuario" class="navbar-icon">
                <i class="bi bi-calendar2-week"></i>
            </a>
            <span class="navbar-title"><strong>LABSOUT</strong></span>
        </div>
    </nav>
    <div class="main-content">
        <h1>LABORATÓRIOS</h1>
        <p class="subtitle">Escolha um laboratório:</p>
        <div class="button-container">
            <div class="button-lab">
                <% if (ambientes && ambientes.length > 0) { %>
                    <% ambientes.forEach(ambiente => { %>
                        <div class="button-wrapper">
                            <button onclick="window.location.href='/calendario/<%= ambiente.id_ambiente %>'"><%= ambiente.nome_ambiente %></button>
                            <!-- Botão "Ver Mais" que aparece ao passar o mouse no botão principal -->
                            <a href="/descricao" class="ver-mais">Ver Mais</a>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p>Nenhum ambiente cadastrado.</p>
                <% } %>
            </div>
        </div>
        
    </div>
</body>
</html>
